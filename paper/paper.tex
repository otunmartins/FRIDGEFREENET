% =============================================================================
% Canonical insulin proposal (fairmeta): ../docs/proposal.tex
% LangChain / OpenMM active-learning paper (fairmeta): ./apssamp.tex  ->  ./compile_apssamp.sh
% Shared bibliography: ./references.bib
%
% Build proposal from the docs/ directory:
%   pdflatex proposal.tex
%   bibtex proposal
%   pdflatex proposal.tex
%   pdflatex proposal.tex
%
% Or run: ../docs/compile_proposal.sh
%
% The former REVTeX full draft was removed to avoid two diverging copies; use
% git history to recover it if needed. This file is not a standalone LaTeX job.
% =============================================================================
